# Marketplace Valkey. Redis protocol, not the Redis image.
# Bind 127.0.0.1 because oracle-api and rails sync use host networking.
bind 127.0.0.1
port 6379
protected-mode yes
maxmemory 32mb
maxmemory-policy allkeys-lru
save ""
appendonly no
tcp-keepalive 60
timeout 0
